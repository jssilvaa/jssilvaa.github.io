<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jssilvaa.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jssilvaa.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-16T15:19:04+00:00</updated><id>https://jssilvaa.github.io/feed.xml</id><title type="html">blank</title><subtitle>Researcher. Embedded systems, robotics, control. </subtitle><entry><title type="html">Chaitin Register Allocation</title><link href="https://jssilvaa.github.io/blog/2026/chaitin-register-allocation/" rel="alternate" type="text/html" title="Chaitin Register Allocation"/><published>2026-05-16T00:00:00+00:00</published><updated>2026-05-16T00:00:00+00:00</updated><id>https://jssilvaa.github.io/blog/2026/chaitin-register-allocation</id><content type="html" xml:base="https://jssilvaa.github.io/blog/2026/chaitin-register-allocation/"><![CDATA[<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">title</span><span class="pi">:</span> <span class="s">Your Compiler's Register Problem Is a Coloring Problem</span>
<span class="na">date</span><span class="pi">:</span> <span class="s">2026-05-16</span>
<span class="na">tags</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">embedded/compilers</span><span class="pi">,</span> <span class="nv">math/optimization</span><span class="pi">]</span>
<span class="na">publish</span><span class="pi">:</span> <span class="kc">true</span>
<span class="nn">---</span>
</code></pre></div></div> <h2 id="the-problem">The Problem</h2> <p>You’ve written a tight inner loop. It has 40 live values. Your target has 32 registers. Something has to go to memory — but <em>what</em>, and <em>when</em>? Pre-Chaitin compilers answered this with local heuristics bolted on after the register assignment already failed: spill the most recently used value, or the one with the longest live range, or whatever the implementer guessed would hurt least. The result was redundant load/store pairs in the worst place possible — inside the hot path — and a compiler architecture where the spill logic was decoupled from the allocation logic, so they couldn’t reason about each other. You paid in code quality and in compile time.</p> <h2 id="the-idea">The Idea</h2> <p>Chaitin’s insight is that register allocation <em>is</em> graph coloring, exactly, and that spilling belongs inside the coloring algorithm, not after it.</p> <p>Build an <strong>interference graph</strong> $G = (V, E)$: one node per symbolic register, an edge between two nodes if they’re ever simultaneously live. Now, assigning $k$ physical registers is equivalent to $k$-coloring $G$ — two nodes sharing an edge can’t share a color. The coloring algorithm is simple: find any node with fewer than $k$ neighbors, remove it, recurse, then reinsert and assign it any color not taken by its neighbors. A free color is always guaranteed to exist because it has fewer than $k$ neighbors.</p> <p>When every node has $k$ or more neighbors, the algorithm is <em>blocked</em>. This is where Chaitin integrates spilling: instead of bailing out, pick the node $v^*$ that minimizes</p> \[v^* = \arg\min_{v} \frac{\text{cost}(v)}{\deg(v)}\] <p>where $\text{cost}(v)$ weights each definition and use of $v$ by estimated execution frequency (approximated as $10^{\text{loop depth}}$), and $\deg(v)$ is how much interference pressure that register is exerting. Spill it: replace the global live range with short-lived temporaries — load before each use, store after each definition — rebuild the graph, and retry. The retry graph is strictly smaller. Repeat until coloring succeeds.</p> <p>The cost/degree ratio captures exactly the right tradeoff: you want to evict the register that relieves the most pressure (high degree) for the least runtime cost (low execution-weighted use count). Loop-invariant values that can be recomputed fall to the bottom of the list immediately; values used in every iteration stay protected.</p> <h2 id="why-its-non-obvious">Why It’s Non-Obvious</h2> <p>The naive move is to separate concerns: do liveness analysis, do allocation, and if it fails, spill greedily and restart. Every compiler before this did exactly that. The problem is that spill decisions made <em>outside</em> the coloring loop can’t see which nodes are actually causing the blockage or how much pressure each one contributes. You end up spilling something that doesn’t unblock the graph, or spilling a hot value when a cold one would have done.</p> <p>The non-obvious move is recognizing that the degree of a node in the interference graph is already the right measure of allocation pressure — it’s not a proxy, it’s the actual constraint. Dividing cost by degree turns a graph property into a spill priority queue, and doing it <em>inside</em> the simplification loop means every spill decision is made with current graph structure, not a stale snapshot from before coloring started.</p> <h2 id="caveats-worth-knowing">Caveats Worth Knowing</h2> <p><strong>The loop-depth frequency proxy is a heuristic.</strong> Weighting by $10^{\text{depth}}$ works well for regular loop nests but mispredicts for cold branches inside hot loops, or any control flow that doesn’t nest cleanly. Profile-guided optimization replaces this proxy with measured frequencies — straightforward in principle, but it requires PGO infrastructure and a representative workload.</p> <p><strong>Coalescing is conservative.</strong> Chaitin merges copy-related registers ($r_i \leftarrow r_j$) only when they don’t already interfere. He never breaks an existing interference to enable a merge. On SSA-form IR with many $\phi$-copies, this leaves copy-elimination on the table. George &amp; Appel’s iterated coalescing (1996) addresses this, but it’s a substantially more complex algorithm.</p> <table> <tbody> <tr> <td><strong>The memory footprint is quadratic.</strong> The interference graph is stored as a $</td> <td>V</td> <td>\times</td> <td>V</td> <td>$ bit matrix for $O(1)$ adjacency queries. For large functions with many symbolic registers, this becomes the binding constraint. Chaitin notes it explicitly — it’s not a theoretical concern, it’s a practical one on the machines of 1982, and it resurfaces today in functions with very large register pressure (e.g., auto-vectorized code with wide SIMD temporaries).</td> </tr> </tbody> </table> <h2 id="what-id-explore-next">What I’d Explore Next</h2> <p>The obvious extension is swapping the loop-depth proxy for real profile data and measuring how much spill quality improves on irregular control flow — the kind you see in state machines or parsers, where the hot path isn’t a clean loop nest. My prior is that the gains are significant and that most compilers are leaving code quality on the floor here.</p> <p>The other thread is the interaction with register-classed architectures: modern targets have separate integer, FP, and vector register files, which partition the interference graph into disconnected subgraphs with different $k$ for each. The cost/degree heuristic should generalize cleanly, but the coalescing logic gets more complicated when a copy crosses class boundaries (e.g., a float moved into a vector register). Worth working through against a real RISC-V V-extension backend.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[--- title: Your Compiler's Register Problem Is a Coloring Problem date: 2026-05-16 tags: [embedded/compilers, math/optimization] publish: true ---]]></summary></entry></feed>